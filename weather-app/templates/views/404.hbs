<!DOCTYPE html>
<html lang="en">
{{>headElement}}
<body>
  {{>navbar}}
  <div class="error-container">
    <div class="error-img"><i class="fas fa-exclamation-triangle fa-xs"></i></div>
    <h1>{{title}}</h1>
    <p>{{body}}</p>
  </div>
  {{>footer}}
</body>
</html>